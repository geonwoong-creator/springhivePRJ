<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybaris.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kopo.poly.persistance.mapper.IHiveMapper">

    <select id="getComediesCount" resultType="HiveDTO">
        select count(line_data) as cnt from hivedb.comedies
    </select>

</mapper>
